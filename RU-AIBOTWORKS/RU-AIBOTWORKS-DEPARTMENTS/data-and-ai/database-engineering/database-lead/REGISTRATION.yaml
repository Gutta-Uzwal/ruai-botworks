# STD-AGENT-001 §34.1 registration record.
# No agent runs in production without one. Reviewed like code.
# GENERATED from RU-AIBOTWORKS-REGISTRY. Do not hand-edit.

agent: database-lead
person: Aiko
owner: data-and-ai / data-architect
environment: development
purpose: Sequence schema work and hold grain and retention decisions steady
classification: internal-high
autonomy_level: L1-supervised
model: opus
tools:
- repository.read
- repository.search
- observability.read
- database.query
- repository.write
- repository.edit
- branch.commit
- record.soft_delete
max_tier: 1
data_domains:
- schema
- client-data
tenancy: per-request, injected by runtime, never from model output
reports_to: data-architect
decision_level: L2
blast_radius: 'Writes only inside its own workspace branch over schema, client-data. Every
  change is Tier 1 and reversible with ''git checkout''. Worst case if fully hijacked: up
  to 50 bad file mutations on one branch, all of which merge-authority reviews before anything
  merges and none of which reach any environment. Cannot write its own configuration. No egress.'
budgets:
  steps: 60
  tokens: 400000
  cost_minor_per_day: 5000
  mutations: 50
kill_switch: global + per-agent
evaluation: EVALUATION.yaml@v1
incident_clocks:
- GDPR Art.33 72h
review_due: '2027-03-15'
registered_on: '2026-09-14'
registered_by: hr-director / agent-registrar
